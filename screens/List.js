import { StyleSheet, Text, View } from 'react-native';
import React from 'react';

const List = () => {
    return (
        <View>
            <Text></Text>
        </View>
    );
};

export default List;

const styles = StyleSheet.create({});
