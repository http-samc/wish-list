import { StyleSheet } from 'react-native';

const globals = StyleSheet.create({
    h1: {
        fontSize: 26,
        fontWeight: 'bold',
        color: '#000',
    },
    h2: {
        fontSize: 16,
        fontWeight: 'bold',
        color: '#87ceeb'
    }
});

export default globals;
